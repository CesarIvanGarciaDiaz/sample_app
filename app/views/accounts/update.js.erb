<% if @account.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody tr#<%= @account.id %>').replaceWith(' <%= j render "row", account: @account %>');

  $('#account-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @account.errors.full_messages[0] %>');
<% end %>
